{{- if .Values.networkPolicy.enabled }}
apiVersion: {{ include "github-rate-limit-checker.networkPolicy.apiVersion" . }}
kind: NetworkPolicy
metadata:
  name: {{ include "github-rate-limit-checker.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "github-rate-limit-checker.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "github-rate-limit-checker.selectorLabels" . | nindent 6 }}
  policyTypes:
    {{- toYaml .Values.networkPolicy.policyTypes | nindent 4 }}
  {{- with .Values.networkPolicy.ingress }}
  ingress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.networkPolicy.egress }}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
